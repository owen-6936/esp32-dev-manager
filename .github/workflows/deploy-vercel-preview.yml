name: Vercel Preview

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    outputs:
      vercel_url: ${{ steps.deploy.outputs.vercel_url }}
      pr_number: ${{ github.event.pull_request.number }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Deploy to Vercel
        id: deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
        run: |
          if [ -z "$VERCEL_TOKEN" ] || [ -z "$VERCEL_PROJECT_ID" ] || [ -z "$VERCEL_ORG_ID" ]; then
            echo "Missing required Vercel secrets"
            exit 1
          fi

          vercel link --cwd . --yes --project $VERCEL_PROJECT_ID --scope $VERCEL_ORG_ID --token $VERCEL_TOKEN
          vercel pull --yes --environment=preview --token $VERCEL_TOKEN
          vercel build
          vercel_url=$(vercel deploy --prebuilt --cwd . --token $VERCEL_TOKEN)

          echo "vercel_url=$vercel_url" >> $GITHUB_OUTPUT
          echo "VERCEL_URL=$vercel_url" >> $GITHUB_ENV

      - name: Update README with Preview URL
        run: |
          sed -i "s|<!--PREVIEW_URL-->|[Preview Deployment](${VERCEL_URL})|g" README.md

      - name: Commit README update
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add README.md
          git commit -m "Update README with Vercel preview URL"
          git push origin HEAD:${{ github.head_ref }}

  comment:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - name: Post Vercel Preview Comment
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ needs.build-and-deploy.outputs.pr_number }}
          comment-id: vercel-preview-${{ needs.build-and-deploy.outputs.pr_number }}
          edit-mode: replace
          body: |
            🚀 **Vercel Preview Available**
            🔗 [View Live Preview](${{ needs.build-and-deploy.outputs.vercel_url }})

            📱 Scan on mobile:
            ![QR Code](https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=${{ needs.build-and-deploy.outputs.vercel_url }})

            🧪 This preview is auto-generated for PR #${{ needs.build-and-deploy.outputs.pr_number }}.
